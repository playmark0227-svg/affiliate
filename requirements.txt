# Needs a recent SDK for adaptive thinking, effort, and structured outputs.
# (Older SDKs still run — llm.py falls back to a basic call.)
anthropic>=0.69.0
jinja2>=3.1.0
markdown>=3.5.0
pyyaml>=6.0
python-frontmatter>=1.1.0
requests>=2.31.0
feedparser>=6.0.0
python-slugify>=8.0.0
beautifulsoup4>=4.12.0
tweepy>=4.14.0
